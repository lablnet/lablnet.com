---
const { title, subtitle, coverPic = null} = Astro.props;
---

<div class="container">
	<section class="py-10 px-5">
		{
			coverPic && (
				<section class="hero container max-w-screen-lg mx-auto pb-10">
					<img class="rounded object-cover !m-0" src={coverPic} />
				</section>
			)
		}
		<section>
			{
				title && (
					<div class="container">
						<h1 class="title dark:text-gray-300">{title}</h1>
						{subtitle && <p class="prose ml-2 text-justify mb-6 dark:text-gray-300">{subtitle}</p>}
					</div>
				)
			}
			<hr class="mt-9 mb-4" />
			<slot />
		</section>
	</section>
</div>
